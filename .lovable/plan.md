

# خطة: تحسين شكل وأداء جدول تحليل التكاليف

## المشاكل الحالية
1. أعمدة الجدول ضيقة جداً (عرض الإنتاجية 80px، الإيجار 60px) مما يقطع النصوص والأرقام
2. حقول الإدخال صغيرة (`w-16`, `w-14`) ولا تظهر الأرقام بالكامل
3. عمود اسم العمل (160px) لا يكفي لعرض النصوص العربية الطويلة
4. التكلفة/م3 تظهر في Badge صغير يصعب قراءته
5. قيم AI والنسب المئوية مزدحمة وصعبة القراءة

## الحلول المقترحة

### الملف: `src/pages/CostAnalysisPage.tsx`

**1. توسيع أعمدة الجدول الافتراضية:**

| العمود | القديم | الجديد |
|---|---|---|
| drag | 30 | 35 |
| workItem | 160 | 220 |
| productivity | 80 | 110 |
| aiProductivity | 100 | 120 |
| dailyRent | 60 | 100 |
| aiRent | 100 | 120 |
| costPerUnit | 80 | 110 |
| actions | 80 | 80 |

**2. تحسين حقول الإدخال في SortableRow:**
- توسيع input الاسم: إزالة `h-7 text-sm` واستبدالها بـ `h-8 text-sm min-w-[180px]`
- توسيع input الإنتاجية: من `w-16` إلى `w-24`
- توسيع input الإيجار: من `w-14` إلى `w-24`
- تنسيق الأرقام بالفاصلة (comma-separated) في Badge التكلفة

**3. تحسين عرض قيم AI:**
- توسيع أزرار AI Suggestion لتظهر الرقم بالكامل مع padding مناسب
- تكبير حجم Badge النسبة المئوية من `text-[9px]` إلى `text-[10px]`
- إضافة `min-w-[50px]` لأزرار الاقتراح

**4. تحسين Badge التكلفة/م3:**
- تكبير الحجم من `text-xs px-2 py-0.5` إلى `text-sm px-3 py-1`
- إضافة خلفية أوضح للتمييز
- عرض الرقم بتنسيق واضح مع فواصل الآلاف

**5. تحسين عام:**
- إزالة `table-fixed` لتتمدد الأعمدة طبيعياً
- إضافة `whitespace-nowrap` للخلايا الرقمية لمنع التفاف الأرقام
- تحسين ارتفاع الصفوف لراحة بصرية أفضل
- إضافة alternating row colors خفيفة (striped rows)

**6. تحسين الأداء:**
- إزالة `console.log` في useEffect (سطر 402-404) الذي يطبع كل البنود عند كل تغيير
- إزالة `console.log` في handleAddNewItem (سطر 548-549)

**7. إضافة اقتراحات جديدة:**
- اقتراح "وسّع أعمدة الجدول لقراءة أفضل" عندما تكون الأعمدة بالحجم الافتراضي
- اقتراح "استخدم تصدير Excel لمشاركة التحليل مع الفريق" عندما يوجد أكثر من 3 بنود

### الملفات المتأثرة:
- `src/pages/CostAnalysisPage.tsx` فقط

